. The Initial Upload (Writing Data)When a user creates a pool and uploads a photo:StepActionService UsedSecurity1Upload Photo (e.g., in PO_AddPool)Local device storageN/A2Generate SignatureYour Firebase Cloud FunctionSecure! The secret API key never leaves the server.3Signed UploadCloudinaryUses the secure signature and API Key to authenticate the upload.4Save URLFirebase FirestoreThe app saves the permanent Cloudinary PhotoURL (e.g., https://res.cloudinary.com/...).2. The Subsequent Download (Reading Data)When the user (or a Service Provider) views the image later:StepActionResultSpeed/DataCheck Local CacheThe app tries to load the image into the view using a library like Glide or Picasso.If found (still on the device), it loads immediately.Fastest, Zero Data Use.Fetch from CloudIf not found locally, the loading library sees the Cloudinary PhotoURL.The library downloads the image, displays it, and saves a copy to its local cache.Slower, Uses Data.Yes, we will use this logic (fetch from local cache first, then download from cloud) throughout the application.